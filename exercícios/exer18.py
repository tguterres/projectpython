# Seno, cosseno e tangente
import math
ang = float(input('Ângulo: '))
sen = math.sin(ang)
cos = math.cos(ang)
tag = math.tan(ang)
print('O seno é {:.1f}, o cosseno é {:.1f}, a tangente é {:.1f}'.format(sen, cos, tag))
